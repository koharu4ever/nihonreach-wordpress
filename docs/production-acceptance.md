# 生产验收记录

检查时间：2026-09-05 14:43:59 UTC。部署提交：`462d481`。站点：[wp.kral-koharu.com](https://wp.kral-koharu.com/)。仓库随后已改为公开。

维护者通过 Coolify Docker Compose 部署独立 web 与 MySQL 容器，运行状态为 healthy；首次初始化创建演示产品和独立管理员。产品数据库及上传图片使用持久卷，源码由镜像提供。

## 已实际检查

| 检查 | 结果 |
|---|---|
| HTTPS 与 HTTP 跳转 | HTTPS 请求成功；HTTP 最终跳转 HTTPS |
| 首页、目录、第二页、产品详情 | 均返回 HTTP 200 |
| 公开产品分页 | 第一页 9 项，第二页 3 项 |
| 产品上传图片 | HTTP 200 |
| 管理员登录 | 登录成功，后台产品菜单可用 |
| 密码保护产品 | 未登录响应不包含测试型号 |
| 不存在的页面 | HTTP 404 |

以上来自上线时实际 HTTP 请求、登录验证和 Coolify 状态读取，不是持续监控或未来可用性保证。不公开登录凭据、会话或数据库备份。

## 验证范围

生产检查未包含压力测试、渗透测试、多浏览器矩阵或线上灾难恢复演练。异地备份尚未配置。容器重建后数据保留是在隔离的本地生产镜像测试中验证；完整备份恢复演练也在本地完成，不能算作线上恢复实测。

本地的 36 项集成断言、安全规则检查和浏览器操作见 [本地验收](acceptance.md)；部署方法见 [Coolify 指南](coolify-deployment.md)。
