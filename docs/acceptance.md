# 本地验收与已知边界（历史记录）

本文保留首次本地验收结果，不代表后续生产部署的验收。当前仓库已公开、线上站点已部署，见 [生产验收记录](production-acceptance.md)。

日期：2026-09-05。工作目录 `C:\dev\nihonreach-wordpress`；旧 Laravel 仅只读参考。

后续已补充 VS Code Dev Container，容器工具及原站点复用验证见 [Dev Container 验证记录](devcontainer-validation.md)。原有业务代码未因这次改造而改变。

2026-09-05 公开展示整理后复验：7 个 PHP 文件仅调整排版，忽略空白和可选尾逗号后的 PHP 标记与 HTML 内容对比一致；PHP 语法、现有 PHPCS 安全规则及隔离数据库中的 36 项集成断言再次通过。

## 自动检查

- `tests/integration.php`：36 个断言真实通过，原始输出在 integration-results.txt。包括 CPT、taxonomy、save_post 合法/非法/空/缺失/数组/过长值、无效 nonce、订阅者无对象能力、编辑角色能力、停用与重新启用、匿名公开目录及第二页、草稿/私密/密码保护详情。
- PHP 8.3.33：自定义插件和主题全部 PHP 文件语法通过。
- PHPCS 3.13.6 + WPCS 3.4.1：tests/phpcs.xml 中的输入验证、输出转义、SQL 与危险函数规则通过。未运行全套 WordPress 样式规范。
- HTTP 12 条路由检查：结果见 http-results.json；单页 canonical 仅一个，归档及分页为零，没有重复输出或第二页错指首页。
- 官方核心 checksums 通过；仅提示额外 `wp-config-docker.php`，这是官方 Docker 镜像附带的配置模板，不是自定义核心修改。
- 再次运行 seed 输出 already applied，保留已有编辑。容器重建后数据及图片保留。

## 浏览器实际验证

- 中文后台登录；经典产品编辑器保存合法型号。
- 非法型号显示中文红色反馈，旧型号仍回显；恢复正确演示型号。
- 首页、目录 9＋3 分页、产品详情图片、空分类与 404。
- 退出后密码保护详情只显示密码表单，不显示型号和正文。
- 390px 手机目录无横向溢出（document scrollWidth 375 ≤ viewport 390），单列卡片，菜单展开和关闭正常。
- 恢复站登录、读型号、修改并保存、前台新型号及图片访问。

## 限制

没有进行多浏览器兼容矩阵、真实移动设备测试、负载测试、渗透测试或完整无障碍审计。浏览器未逐一执行每个角色的所有编辑/删除操作；角色能力和保存拒绝由真实 WordPress 账号的集成测试覆盖。前台图片上传的底层媒体导入和显示已验证，浏览器的手动文件选择上传流程未单独回放。

自动测试不等于 WordPress 全部功能安全；仅覆盖作品的自定义范围。型号不是唯一 SKU，业务保存不是事务回滚。WordPress 附件是公开 URL，密码保护不对媒体文件进行加密。无订单、表单提交、真实邮件、支付或与 Laravel 的同步。首次本地验收不包含生产部署；后续生产检查另行记录。
